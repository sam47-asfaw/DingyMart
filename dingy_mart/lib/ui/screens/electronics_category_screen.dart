import 'package:flutter/material.dart';

class ElectronicsCategoryScreen extends StatelessWidget {
  const ElectronicsCategoryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Accessories'),
    );
  }
}
