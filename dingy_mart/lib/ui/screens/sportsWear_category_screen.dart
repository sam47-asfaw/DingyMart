import 'package:flutter/material.dart';

class SportsWearCategoryScreen extends StatelessWidget {
  const SportsWearCategoryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
